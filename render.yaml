services:
  - type: web
    name: niseko-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: niseko-project-db
          property: connectionString
databases:
  - name: niseko-project-db
    plan: free
